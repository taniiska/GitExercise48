# Project TODO (smart-hub)

- [x] Center “Go to Login” link on Register page (CSS scoped to `body.register-page`).
- [x] Implement dark-mode facility booking dashboard UI on `/dashboard` (Flask renders `dashboard.html`).
- [x] Add `/profile` page template with required sections (Personal Info, Preferences, Account Settings, Booking Stats).
- [ ] Add `/profile` backend logic for photo upload + real persistence (currently demo via session).
- [ ] Add `/profile` + `/profile/password` routes to properly update password (hash) in SQLite `users` table.
- [ ] Update `static/style.css` with dashboard-matching profile styling classes (`profile-layout`, `profile-card`, etc.).

